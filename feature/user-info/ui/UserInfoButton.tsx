"use client";

import { UserRound } from "lucide-react";

/**
 * 로그인 화면 / 내 정보 화면으로 이동할 수 있는 버튼
 * @returns
 */
const UserInfoButton = () => {
  return (
    <div>
      <UserRound className="size-6" />
    </div>
  );
};

export default UserInfoButton;
